# coverstore configuration

db_parameters:
    dbn: "postgres"
    db: "coverstore"
    host: db

data_root: "/var/lib/coverstore"
default_image: "static/images/empty.gif"
# Dev-only: covers missing from the local store redirect to production.
fallback_url: "https://covers.openlibrary.org"

sentry:
    enabled: false
    # Dummy endpoint; where sentry logs are sent to
    dsn: 'https://examplePublicKey@o0.ingest.sentry.io/0'
    traces_sample_rate: 1.0
    environment: 'local'
